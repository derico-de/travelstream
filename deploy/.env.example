# Copy to .env and adjust.
HTTP_PORT=80

# Dedicated restapi user for the media worker (create it in Plone with
# Editor rights on the trips area).
WORKER_USER=mediaworker
WORKER_PASSWORD=change-me

# Backups
BACKUP_DIR=./backups
BACKUP_INTERVAL_SECONDS=86400
BACKUP_KEEP_DAYS=30
